import template from './terminal.html';
import controller from './terminal.controller';
import './terminal.scss';

let terminalComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default terminalComponent;
